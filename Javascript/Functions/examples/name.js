let string = "sHAnboR"
let fullChar = string.slice(0,1).toUpperCase()+string.slice(1,string.length).toLowerCase();
// var upperChar = firstChar.toUpperCase();
// var restOfChar = name.slice(1,name.length);
// var restOfChar = restOfChar.toLowerCase();

console.log(fullChar);
